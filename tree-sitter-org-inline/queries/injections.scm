; Injection queries for org_inline grammar
; This grammar can be injected into title nodes from the block grammar

; Currently no injections from inline grammar to other grammars
; In the future, this might inject:
; - LaTeX into inline LaTeX fragments
; - Code into inline code
